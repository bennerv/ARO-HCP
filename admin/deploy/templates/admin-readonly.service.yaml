apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.readonly.service.name }}
  namespace: '{{ .Release.Namespace }}'
  labels:
    app: admin-api-readonly
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.readonly.service.port }}
    targetPort: http
    protocol: TCP
    name: https
  selector:
    app: admin-api-readonly
